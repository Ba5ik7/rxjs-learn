import { MatchUrlSegmentPipe } from './match-url-segment.pipe';

describe('MatchUrlSegmentPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchUrlSegmentPipe();
    expect(pipe).toBeTruthy();
  });
});
